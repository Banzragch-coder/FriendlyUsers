// module.exports = (app) => {
//     const contacts = require("../controllers/user.controller");

//     var router = require("express").Router();

//     router.post("/", contacts.findAllByUser);

//   app.use("/api/user", router);
// };
